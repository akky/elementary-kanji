"use strict";

describe('find and convert elementary school kanji-s', () => {
/*
  describe('hasLetter()', () => {
    it('should be true with 右 in 1st grade', () => {
      assert.equal(true, hasLetter('右', 0));
    });
  });



  describe('replaceLetter()', () => {
    it('should replace 右 with 右(1)', () => {
      assert.equal('右(1)', replaceLetter('右'));
    });
  });

  describe('replaceLetter()', () => {
    it('should not replace です', () => {
      assert.equal('です', replaceLetter('です'));
    });
  });

  describe('replaceLetter()', () => {
    it('should replace 水 with 水(1)', () => {
      assert.equal('水(1)', replaceLetter('水'));
    });
  });




  describe('replaceNode()', () => {
    it('should replace 今日(1)は水曜日 with 今(2)日(1)は水(1)曜(2)日(1)', () => {
      assert.equal('今(2)日(1)は水(1)曜(2)日(1)', replaceNode('今日は水曜日'));
    });
  });

  describe('replaceNode()', () => {
    it('should replace へんかん関数をかえる with 変換関数を変える', () => {
      function span_converter(letter, grade) {
        return '<span class="grade_' + (grade) + '">' + letter + '</span>';
      }

      assert.equal('へんかん<span class="grade_3">関</span><span class="grade_1">数</span>をかえる',
                   replaceNode('へんかん関数をかえる', span_converter)
      );
    });
  });
*/

});
